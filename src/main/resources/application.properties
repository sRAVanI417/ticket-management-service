spring.application.name=ticket-management-service

# Datasource Configuration
spring.datasource.url=jdbc:postgresql://ep-lucky-recipe-a8jufenk-pooler.eastus2.azure.neon.tech/neondb
spring.datasource.username=neondb_owner
spring.datasource.password=npg_SWRE2ULPI9JY
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Ensure data.sql runs after Hibernate creates tables
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
